<div class="container mt-3">
      <table class="table table-hover">
        <tr>
            <th>Category</th>
        </tr>
        {{#each categorys }}
        <tr>
            <td>
                <a {{ _id }}>{{ name }}</a>
            </td>
        </tr>
        {{/each}}
    </table>
    <div class="d-flex justify-content-center" role="group" aria-label="Button Group">
        <a class="btn btn-dark btn-info" href="/category/add">Add new category</a>
    </div>
</div>